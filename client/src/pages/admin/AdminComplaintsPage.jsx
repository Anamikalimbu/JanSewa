import AdminComingSoon from "../../components/admin/AdminComingSoon";

export default function AdminComplaintsPage() {
  return (
    <AdminComingSoon
      title="Complaints"
      description="A full, filterable list of every complaint in the system — with status and department assignment controls — will live here."
    />
  );
}
