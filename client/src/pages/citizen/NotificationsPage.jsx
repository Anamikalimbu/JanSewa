import ComingSoon from "../../components/dashboard/ComingSoon";

export default function NotificationsPage() {
  return (
    <ComingSoon
      title="Notifications"
      description="Updates on your complaints — status changes, department assignments, and resolutions — will show up here."
    />
  );
}
